import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Home - Flashcards',
  description: 'Home Flashcards',
};

const page = () => {
  return <div>Home Page</div>;
};

export default page;
