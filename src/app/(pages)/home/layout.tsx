//TODO: Ejemplo: Si se necesita cambiar el layout se puede modificar en un archivo
//layout.tsx dentro de la carpeta correspondiente
export default function HomeLayout({ children }: { readonly children: React.ReactNode }) {
  return <>{children}</>;
}
